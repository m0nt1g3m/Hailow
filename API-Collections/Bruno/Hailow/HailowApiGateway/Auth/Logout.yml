info:
  name: Logout
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/auth/logout
  body:
    type: json
    data: |-
      {
          "refreshToken": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
