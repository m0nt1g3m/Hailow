info:
  name: Refresh
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/auth/refresh
  body:
    type: json
    data: |-
      {
          "refreshToken": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
